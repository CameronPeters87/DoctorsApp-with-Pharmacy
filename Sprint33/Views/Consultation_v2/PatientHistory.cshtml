@model IEnumerable<Sprint33.Models.Consultation_v2>

@{
    ViewBag.Title = "PatientHistory";
}
<div class="container">

<h2>Patient Records</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PatientName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Appointment.DoctorName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AppointmentDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Symptoms)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Diagnosis)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Notes)
        </th>
    </tr>

    @foreach (var item in Model)
    {
        if (item.PatientName == Session["UserName"].ToString())
        {
<tr>

    <td>
        @Html.DisplayFor(modelItem => item.PatientName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Appointment.DoctorName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.AppointmentDate)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Symptoms)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Diagnosis)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Notes)
    </td>

</tr>}

            }

</table>
<a type="button" class="btn btn-secondary"
   href="@Url.Action("Homepage", "Patients1")">
    <span class="fa fa-home"></span>
</a>
</div>